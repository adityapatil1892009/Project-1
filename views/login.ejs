<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Water Supply Management</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #0077b6 0%, #00b4d8 50%, #03045e 100%);
            position: relative;
            overflow-y: auto;
            padding: 20px;
            font-size: 16px;
            background-attachment: fixed;
        }

        /* Animated background */
        body::before {
            content: '';
            position: absolute;
            width: 400px;
            height: 400px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            top: -100px;
            left: -100px;
            animation: float 20s infinite ease-in-out;
        }

        body::after {
            content: '';
            position: absolute;
            width: 300px;
            height: 300px;
            background: rgba(255, 255, 255, 0.05);
            border-radius: 50%;
            bottom: -50px;
            right: -50px;
            animation: float 25s infinite ease-in-out reverse;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(30px); }
        }

        .login-wrapper {
            position: relative;
            z-index: 10;
            width: 100%;
            max-width: 900px;
        }

        .login-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            display: grid;
            grid-template-columns: 1fr 1fr;
            overflow: hidden;
        }

        /* Left side - Info */
        .login-info {
            background: linear-gradient(135deg, #0077b6, #00b4d8);
            padding: 60px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            color: white;
        }

        .login-info-icon {
            font-size: 4rem;
            margin-bottom: 20px;
            opacity: 0.9;
        }

        .login-info h2 {
            font-size: 2.2rem;
            margin-bottom: 12px;
            font-weight: 700;
        }

        .login-info p {
            font-size: 1.1rem;
            opacity: 0.9;
            line-height: 1.6;
        }

        .login-info-features {
            font-size: 1.05rem;
        }

        .login-info-features {
            margin-top: 30px;
            font-size: 1rem;
            opacity: 0.85;
        }

        .login-info-features li {
            margin-bottom: 8px;
            list-style: none;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .login-info-features i {
            font-size: 1.2rem;
        }

        /* Right side - Form */
        .login-form-section {
            padding: 60px 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .form-header {
            margin-bottom: 30px;
        }

        .form-header h3 {
            color: #03045e;
            font-size: 2rem;
            margin-bottom: 6px;
        }

        .form-header p {
            color: #666;
            font-size: 1.1rem;
        }

        .form-group {
            margin-bottom: 18px;
        }

        label {
            display: block;
            margin-bottom: 6px;
            font-weight: 600;
            color: #333;
            font-size: 1.05rem;
        }

        select, input {
            width: 100%;
            padding: 12px 12px;
            border: 2px solid #e0e0e0;
            border-radius: 6px;
            font-size: 1.05rem;
            font-family: inherit;
            transition: all 0.3s ease;
        }

        select:focus, input:focus {
            outline: none;
            border-color: #0077b6;
            box-shadow: 0 0 0 3px rgba(0, 119, 182, 0.1);
        }

        .submit-btn {
            width: 100%;
            padding: 12px;
            background: linear-gradient(135deg, #0077b6, #00b4d8);
            color: white;
            border: none;
            border-radius: 6px;
            cursor: pointer;
            font-weight: 700;
            font-size: 1.15rem;
            margin-top: 8px;
            transition: all 0.3s ease;
            min-height: 48px;
        }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 119, 182, 0.3);
        }

        .error-message {
            background: #fee2e2;
            color: #b91c1c;
            padding: 10px 12px;
            border-radius: 6px;
            margin-bottom: 18px;
            border-left: 4px solid #b91c1c;
            font-size: 1.05rem;
        }

        .demo-credentials {
            background: #f5f5f5;
            padding: 14px 12px;
            border-radius: 6px;
            border-left: 3px solid #00b4d8;
            margin-top:1rem;
        }

        .demo-credentials strong {
            color: #0077b6;
            display: block;
            margin-bottom: 6px;
        }

        .demo-credentials small {
            color: #666;
            display: block;
            line-height: 1.5;
            font-size: 1.05rem
            line-height: 1.5;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .login-card {
                grid-template-columns: 1fr;
            }

            .login-info {
                padding: 40px 30px;
            }

            .login-form-section {
                padding: 40px 30px;
            }

            .login-info h2 {
                font-size: 1.5rem;
            }

            .login-info-icon {
                font-size: 3rem;
            }

            .form-header h3 {
                font-size: 1.2rem;
            }
        }

        @media (max-width: 480px) {
            body {
                padding: 15px;
            }

            .login-card {
                border-radius: 12px;
            }

            .login-info, .login-form-section {
                padding: 30px 20px;
            }

            .login-info h2 {
                font-size: 1.3rem;
            }

            .login-info-icon {
                font-size: 2.5rem;
            }

            select, input {
                padding: 10px 10px;
                font-size: 16px;
            }
        }

        .lang-select-large {
            padding: 12px;
            font-size: 1.1rem;
            border-radius: 6px;
            border: 2px solid #0077b6;
            background: white;
            color: #333;
            cursor: pointer;
            transition: all 0.3s ease;
            min-height: 48px;
        }

        .lang-select-large:hover {
            border-color: #005f8d;
            box-shadow: 0 4px 8px rgba(0, 95, 141, 0.2);
        }

        .lang-select-compact {
            padding: 8px;
            font-size: 1rem;
            border-radius: 4px;
            border: 2px solid #0077b6;
            background: white;
            color: #333;
            cursor: pointer;
            transition: all 0.3s ease;
            min-height: 40px;
        }

        .lang-select-compact:hover {
            border-color: #005f8d;
            box-shadow: 0 4px 8px rgba(0, 95, 141, 0.2);
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <div class="login-card">
            <!-- Left Side - Info -->
            <div class="login-info">
                <div class="login-info-icon">
                    <i class="fas fa-water"></i>
                </div>
                <h2>Water Supply Management</h2>
                <p>Efficient water supply scheduling and management for Pune Municipal Corporation</p>
                
                <ul class="login-info-features">
                    <li><i class="fas fa-check-circle"></i> Real-time schedules</li>
                    <li><i class="fas fa-check-circle"></i> Complaint tracking</li>
                    <li><i class="fas fa-check-circle"></i> Authority dashboard</li>
                    <li><i class="fas fa-check-circle"></i> Secure access</li>
                </ul>
            </div>

            <!-- Right Side - Form -->
            <div class="login-form-section">
                <div class="form-header">
                    <h3 data-i18n="login.signin">Sign In</h3>
                    <p data-i18n="login.subtitle">Enter your credentials to continue</p>
                </div>

                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="error-message">
                        ❌ <%= error %>
                    </div>
                <% } %>

                <form action="/login" method="post">
                    <div class="form-group">
                        <label for="role" data-i18n="login.role">Role *</label>
                        <select id="role" name="role" required>
                            <option value="" data-i18n="login.role.select">Select your role</option>
                            <option value="citizen">👤 <span data-i18n="role.citizen">Citizen</span></option>
                            <option value="authority">🏛️ <span data-i18n="role.authority">Authority</span></option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="method" data-i18n="login.method">Method *</label>
                        <select id="method" name="method" required>
                            <option value="" data-i18n="login.method.select">Select method</option>
                            <option value="email">📧 <span data-i18n="login.method.email">Email</span></option>
                            <option value="code">🔐 <span data-i18n="login.method.code">Code</span></option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="identifier" data-i18n="login.identifier">Email / Code *</label>
                        <input type="text" id="identifier" name="identifier" placeholder="Enter your credentials" required>
                    </div>

                    <button type="submit" class="submit-btn" data-i18n="login.submit">Sign In</button>
                </form>

                <div class="demo-credentials">
                    <strong>Demo Access:</strong>
                    <small>
                        <strong style="color: #666; font-weight: 600;">Citizen:</strong> ramesh.patil@example.com / ZONE-101<br>
                        <strong style="color: #666; font-weight: 600;">Authority:</strong> admin@pmc.gov.in / PMC-AUTH-001
                    </small>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
